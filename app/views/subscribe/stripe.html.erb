<%= form_tag subscribe_path(1), :method => :put do %>
	<article>
		<label for="" class="amount">
			<span>Amount: 4.99</span>
		</label>
	</article>

	<script src="https://checkout.stripe.com/v2/checkout.js" class="stripe-button" data-key="<%= Rails.configuration.stripe[:publishable_key] %>" data-description="A month's subscription" data-amount="499"></script>
<% end %>